package Smoother;

import static org.junit.Assert.*;

import org.junit.Test;

import Salter.Salting;
/**
 * This is the tester class for the smoother and this class calls it through the main method and runs the code 
 * @author Melvin Vazquez
 *
 */
public class TestSmoothing {
	/**
	 * This is the main method that is used to print out results from the plotter class
	 * @param args
	 */
	public static void main(String[]args) {
		Smoothing test = new Smoothing();
		test.run();
	}

}
