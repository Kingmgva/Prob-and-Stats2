package Salter;

import static org.junit.Assert.*;

import org.junit.Test;
/**
 * This is the tester class for the salter and this class calls it through the main method and runs the code 
 * @author Melvin Vazquez
 *
 */
public class TestSalting {
	/**
	 * This is the main method that is used to print out results from the plotter class
	 * @param args
	 */
	public static void main(String[]args) {
		Salting test = new Salting();
		test.run();
	}
}
